4) Write a java program to take array length as input from user and 
create an integer array Create an integer array and store elements 
into the array by taking elements as input from keyboard. Before 
Swapping print the contents of array Ask user to enter 2 indexes.
Swap the the values of 2 indexes After swapping print the contents of 
array. 

Explanation:
first create the integer array and taking the length from user
store the elements in an  array by taking elements from the keyboard
Before swapping printing the contents of array
Asking the two indexes from user and swapping the values in the indexes
After swapping the values printing the contents of array
